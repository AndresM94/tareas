const alturaInt = 181;

const alturaFloat = 1.81;

const pesoKg = 57.4;

const alturaRedondeado = Math.ceil(alturaFloat);

const pesoRedeondeado = Math.floor(pesoKg);

const iguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
